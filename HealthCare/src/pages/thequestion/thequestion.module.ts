import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ThequestionPage } from './thequestion';

@NgModule({
  declarations: [
    ThequestionPage,
  ],
  imports: [
    IonicPageModule.forChild(ThequestionPage),
  ],
})
export class ThequestionPageModule {}
